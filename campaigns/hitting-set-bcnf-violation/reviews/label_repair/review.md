# Focused independent re-review of `d170521`

**Decision: advance.** The repair closes the initial review's blocking source-domain defect. The reduction now constructs a legal target for arbitrary explicit element names admitted by its source JSON contract and decodes every valid target output to a bounded hitting set or the correct NO-SOLUTION answer. This means eligible for expert review, not publication acceptance.

## Correctness of the repair

The initial [review](../initial/review.md) found that `U=[2,4]`, `E=[[4]]`, `k=1` produced FDs mentioning absent `a0,a1`. The unchanged [reviewer check](../initial/check_domain.py) now exits 0: `A` contains `a0,a1`, and every FD side is a subset of `A`. The repair in `work/algorithm.py:8-9,17-19` assigns position `i` to each input member and uses `a_i` consistently for incidences and the threshold circuit. Distinct string and integer members are distinct dictionary keys under the stated contract; booleans and floats are excluded. Since every edge member belongs to the universe, all incidence lookups exist. All other generated FD names and `R` members are constructed from these indices, so forward legality holds for every admitted source input, including empty universes and `k>=n`.

`work/algorithm.py:36-43` now maps selected `a_i` back to the original input member in universe order and checks the size and every family set. Its NO-SOLUTION branch is correct for every **valid** negative target output because the target has a violation exactly when the source has a bounded hitting set. The repaired `work/proof.md:3,7-21` supplies that equivalence with indexed elements: before `C,D` complete, `b_j` records whether the selected original members meet `E_j`, and `t_{n,k+1}` records whether their count exceeds `k`. The unchanged closure classification from the initial review therefore applies to every `X⊆R`, independent of label spelling or order. For any partial closure, neither marker is in `X`, and its element indices decode to a hitting set of size at most `k`; `y,z` may be any valid pair. Conversely every bounded hitting set gives `(X,C,D)`. The explicit NO-SOLUTION output makes both target and source output sets nonempty. This addresses all valid target outputs, not just the tested ones.

The reviewer-owned [focused check](check_labels.py) invokes the real forward and extract processes, checks target legality, independently enumerates source hitting sets and **all** target witnesses by closure on three labelled sources, then checks every recovery. Results: `[2,4]` positive has 1 source solution and 2 target outputs; `[2,4]` negative has neither; mixed labels `["z",-7,"2"]` have 2 source solutions and 4 target outputs. All checks passed. The repaired prepared loop reports 125 inputs/202 target outputs and the separate Z3 target loop 52 inputs/77 outputs in `work/verification.md`; their labelled additions and use of actual target outputs were inspected in `work/check.py` and `work/verify.py`. I reused the unaffected canonical-domain evidence from the initial review and did not rerun those suites. Finite checks support the implementation but are not the general proof.

The revised bounds in `work/proof.md:25` account for original label bits in input length `L`, the `n≤L` index map, the `O(n²+s+m)` FDs and `O(log(n+m+2))` internal names. Forward output length is `O((n²+s+m)log(n+m+2))⊆O(L²log(L+2))`. Recovery returns at most `n` original labels, of total size at most their appearance in `L`, and scans the source and output in polynomial time. Both maps are deterministic and make no solver call. The lingering `{a_i:i∈U}` at `work/proof.md:9` should read `{a_i:0≤i<n}` because `U` now contains labels, not indices; it is a notation slip, since `algorithm.py:32` and `proof.md:7` state the indexed rule unambiguously. It does not affect the argument.

## Novelty and significance

The label repair changes neither gadget nor theorem. The initial review's [primary comparison](../initial/review.md#novelty-and-literature-checked-2026-09-23) remains applicable: [Bernstein–Beeri 1976, Part III §2, Theorem 8](https://ia800105.us.archive.org/24/items/technicalreportc73univ/technicalreportc73univ.pdf) uses an exact-one-per-set variant without a binary cardinality bound; [Vardi, Chapter 5, Theorem 5.9](https://www.cs.rice.edu/~vardi/papers/ttcs87.pdf) reproduces that version; [Garey–Johnson, Appendix A4, SR29](https://perso.limos.fr/~palafour/PAPERS/PDF/Garey-Johnson79.pdf) lists a Vertex Cover transformation for the same target predicate. The [1979 journal version](https://doi.org/10.1145/320064.320066) was not accessible in full, and later bounded variants have not been exhaustively ruled out. The result claims a complete reproducible at-most-`k` construction and all-output recovery, not precedence or a new complexity classification; the fixed question permits reconstruction of a published result. In that scope, novelty is adequately characterized, with unresolved literature coverage explicit.

The practical contribution remains an executable reduction with closure-checkable target witnesses and exact recovery for labelled source instances. Threshold attributes and FDs are `Θ(n·min(n,k+1))` in the worst cases, so quadratic growth can dominate construction and target solving. Only small finite cases were executed, and no practical runtime or memory claim was measured. The fixed question sets no tighter overhead requirement; this cost does not block advance.

## Review boundary

This was a follow-up by the registered `research-reviewer`, using the prior report and repair record while reassessing the actual current files. The exposed model route is the inherited GPT-6 family route; exact backend variant/version was unavailable. No tool-layer denial of collaboration tools or structural depth cap was exposed, and filesystem sandboxing was `danger-full-access`; isolation of writes and no delegation were instruction-only. I did not spawn agents and wrote only in this `label_repair` review directory. Boundary mechanisms are separate from correctness evidence.
